package com.pgd.genericFilter;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GenericFilterServiceApplication {

    /**
     * Static method to start the spring boot application.
     *
     * @param args array of {@link String} of values pass to run the application
     */
    public static void main(final String[] args) {
        SpringApplication.run(GenericFilterServiceApplication.class, args);
    }

}
